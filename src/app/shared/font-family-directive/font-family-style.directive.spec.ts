import { FontFamilyStyleDirective } from './font-family-style.directive';

describe('FontFamilyStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FontFamilyStyleDirective();
    expect(directive).toBeTruthy();
  });
});
